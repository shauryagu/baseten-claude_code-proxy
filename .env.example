# Required: your Baseten API key (from app.baseten.co)
BASETEN_API_KEY=your_baseten_api_key

# Optional: override defaults
# OPENAI_BASE_URL=https://inference.baseten.co/v1
# TARGET_MODEL=moonshotai/Kimi-K2.5

# Optional: leave unset for local use. If set, Claude Code must send this as Bearer or X-Api-Key.
# PROXY_AUTH_KEY=
